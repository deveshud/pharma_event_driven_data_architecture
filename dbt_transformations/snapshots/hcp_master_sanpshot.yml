snapshots:
  - name: hcp_master_snapshot
    relation: ref('hcp_master_ephemeral')
    config:
      schema: snapshots
      alias: hcp_master_snapshot
      strategy: check
      unique_key: hcp_id
      check_cols: [hcp_id, specialty,territory,engagement_tier]
#      dbt_valid_to_current: true
      hard_deletes: ignore